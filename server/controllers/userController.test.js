describe("Given the getUser function", () => {
  describe("When it's called", () => {
    test("Then it should return a user 'Luis' and his password '1q2w'", async () => {
      // const expectedUser = { name: "Luis", password: "1q2w" };
      // const userReceived = await getUser();
      // expect(userReceived).toEqual(expectedUser);
    });
  });
});
